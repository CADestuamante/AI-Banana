[project]
name = "banana-ai"
version = "0.1.0"
description = "Desktop AI system for banana ripeness detection and quality assessment."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
